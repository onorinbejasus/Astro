// Generated by CoffeeScript 1.3.2
var Projection,
  __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

Projection = (function() {

  function Projection() {
    this.unproject = __bind(this.unproject, this);

    this.getHeader = __bind(this.getHeader, this);

    var coords;
    this.parameters = null;
    this.getHeader("./images/testframe.jpeg");
    coords = this.unproject(499, 499);
  }

  Projection.prototype.getHeader = function(image) {
    var _this = this;
    this.parameters = new Object;
    $.ajaxSetup({
      'async': false
    });
    $.getJSON("./imageHeader.php?url=" + image, function(data) {
      return $.each(data, function(key, val) {
        if (key === "CRVAL_1") {
          _this.parameters.crval1 = val;
        }
        if (key === "CRVAL_2") {
          _this.parameters.crval2 = val;
        }
        if (key === "CRPIX_1") {
          _this.parameters.crpix1 = val;
        }
        if (key === "CRPIX_2") {
          _this.parameters.crpix2 = val;
        }
        if (key === "CD1_1") {
          _this.parameters.cd11 = val;
        }
        if (key === "CD1_2") {
          _this.parameters.cd12 = val;
        }
        if (key === "CD2_1") {
          _this.parameters.cd21 = val;
        }
        if (key === "CD2_2") {
          return _this.parameters.cd22 = val;
        }
      });
    });
    $.ajaxSetup({
      'async': true
    });
  };

  Projection.prototype.unproject = function(xsize, ysize) {
    var dec, decpole, dtor, i, j, l, lat, latpole, long, lonpole, lp, m, mat, mp, n, np, phi, r, r11, r12, r13, r21, r22, r23, r31, r32, r33, ra, rapole, rtod, theta, x, xpix, y, ypix, _i, _j, _k, _l, _m, _n, _o, _results, _results1, _results2, _results3, _results4, _results5, _results6;
    rtod = 57.29577951308323;
    dtor = 0.0174532925;
    xpix = (function() {
      _results = [];
      for (var _i = 0; 0 <= xsize ? _i <= xsize : _i >= xsize; 0 <= xsize ? _i++ : _i--){ _results.push(_i); }
      return _results;
    }).apply(this);
    ypix = (function() {
      _results1 = [];
      for (var _j = 0; 0 <= ysize ? _j <= ysize : _j >= ysize; 0 <= ysize ? _j++ : _j--){ _results1.push(_j); }
      return _results1;
    }).apply(this);
    ra = (function() {
      _results2 = [];
      for (var _k = 0; 0 <= xsize ? _k <= xsize : _k >= xsize; 0 <= xsize ? _k++ : _k--){ _results2.push(_k); }
      return _results2;
    }).apply(this);
    dec = (function() {
      _results3 = [];
      for (var _l = 0; 0 <= xsize ? _l <= xsize : _l >= xsize; 0 <= xsize ? _l++ : _l--){ _results3.push(_l); }
      return _results3;
    }).apply(this);
    _results4 = [];
    for (i = _m = 0; 0 <= xsize ? _m <= xsize : _m >= xsize; i = 0 <= xsize ? ++_m : --_m) {
      ra[i] = (function() {
        _results5 = [];
        for (var _n = 0; 0 <= ysize ? _n <= ysize : _n >= ysize; 0 <= ysize ? _n++ : _n--){ _results5.push(_n); }
        return _results5;
      }).apply(this);
      dec[i] = (function() {
        _results6 = [];
        for (var _o = 0; 0 <= ysize ? _o <= ysize : _o >= ysize; 0 <= ysize ? _o++ : _o--){ _results6.push(_o); }
        return _results6;
      }).apply(this);
      _results4.push((function() {
        var _p, _results7;
        _results7 = [];
        for (j = _p = 0; 0 <= ysize ? _p <= ysize : _p >= ysize; j = 0 <= ysize ? ++_p : --_p) {
          x = this.parameters.cd11 * (xpix[i] - this.parameters.crpix1) + this.parameters.cd12 * (ypix[j] - this.parameters.crpix2);
          y = this.parameters.cd21 * (xpix[i] - this.parameters.crpix1) + this.parameters.cd22 * (ypix[j] - this.parameters.crpix2);
          long = Math.atan2(-y, x);
          lat = (Math.PI / 2.0) * dtor;
          r = Math.sqrt(Math.pow(x, 2), Math.pow(y, 2));
          if (r > lat) {
            lat = Math.atan((180.0 / Math.PI) / r);
          }
          l = Math.cos(lat) * Math.cos(long);
          m = Math.cos(lat) * Math.sin(long);
          n = Math.sin(lat);
          phi = 0.0;
          theta = 90.0 * dtor;
          lonpole = this.parameters.crval2 > 0.0 ? 0.0 : 180.0 * dtor;
          latpole = 90.0 * dtor;
          rapole = this.parameters.crval1 * dtor;
          decpole = this.parameters.crval2 * dtor;
          r11 = -1.0 * Math.sin(rapole) * Math.sin(lonpole) - Math.cos(rapole) * Math.cos(lonpole) * Math.sin(decpole);
          r12 = Math.cos(rapole) * Math.sin(lonpole) - Math.sin(rapole) * Math.cos(lonpole) * Math.sin(decpole);
          r13 = Math.cos(lonpole) * Math.cos(decpole);
          r21 = Math.sin(rapole) * Math.cos(lonpole) - Math.cos(rapole) * Math.sin(lonpole) * Math.sin(decpole);
          r22 = -1.0 * Math.cos(rapole) * Math.cos(lonpole) - Math.sin(rapole) * Math.sin(lonpole) * Math.sin(decpole);
          r23 = Math.sin(lonpole) * Math.cos(decpole);
          r31 = Math.cos(rapole) * Math.cos(decpole);
          r32 = Math.sin(rapole) * Math.cos(decpole);
          r33 = Math.sin(decpole);
          mat = [[r11, r12, r13], [r21, r22, r23], [r31, r32, r33]];
          lp = mat[0][0] * l + mat[1][0] * m + mat[2][0] * n;
          mp = mat[0][1] * l + mat[1][1] * m + mat[2][1] * n;
          np = mat[0][2] * l + mat[1][2] * m + mat[2][2] * n;
          dec[i][j] = Math.asin(np) * rtod;
          ra[i][j] = Math.atan(mp, lp) * rtod;
          if (ra[i][j] < 0.0) {
            _results7.push(ra[i][j] += 360.0);
          } else if (ra[i][j] > 360.0) {
            _results7.push(ra[i][j] -= 360);
          } else {
            _results7.push(void 0);
          }
        }
        return _results7;
      }).call(this));
    }
    return _results4;
  };

  return Projection;

})();
